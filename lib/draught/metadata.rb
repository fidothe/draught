require_relative 'metadata/instance'
require_relative 'metadata/methods'

module Draught
  # Provides common methods for dealing with metadata (like name) for path- and
  # box-like objects, along with a metadata instance class for storing them
  module Metadata
  end
end